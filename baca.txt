// Creating a database model
from posts.models import Post

new_post1=Post(
    title="REACTJS for haters",
    content="This is a video for React haters",
)
new_post1.save()

new_post2=Post(
    title="ANGULAR for haters",
    content="This is a video for Angular haters",
)
new_post2.save()

new_post3=Post(
    title="Vue for haters",
    content="This is a video for Vue haters",
)
new_post3.save()

Post.objects.all()


// Serializing database model instances
from posts.serializers import PostSerializer
from posts.models import Post
from rest_framework.renderers import JSONRenderer

new_post=Post(
    title="Java for haters",
    content="This is a video for java haters",
)
new_post.save()
serializer=PostSerializer(instance=new_post)
serializer
serializer.data

json_data=JSONRenderer().render(serializer.data)
json_data

// Deserializing JSON
import io
stream=io.BytesIO(json_data)
from rest_framework.parsers import JSONParser
dict_data=JSONParser().parse(stream)
dict_data

